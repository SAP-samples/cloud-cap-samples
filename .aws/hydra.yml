serve:
  cookies:
    same_site_mode: Lax

urls:
  self:
    issuer: https://auth.localhost
  consent: https://login.localhost/consent
  login: https://login.localhost/login
  logout: https://login.localhost/logout

secrets:
  system:
    - youReallyNeedToChangeThis

oidc:
  subject_identifiers:
    supported_types:
      - pairwise
      - public
    pairwise:
      salt: youReallyNeedToChangeThis
