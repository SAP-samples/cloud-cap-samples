{
  "name": "@capire/fiori",
  "version": "1.0.0",
  "dependencies": {
    "@capire/bookshop": "../bookshop",
    "@capire/common": "../common",
    "@capire/orders": "../orders",
    "@sap/cds": ">=3.33.1",
    "express": "^4.17.1",
    "hdb": "^0.18.1",
    "sqlite3": "^5.0.0"
  },
  "scripts": {
    "start": "cds run --in-memory?",
    "watch": "cds watch"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sql"
      }
    }
  }
}
